# `@benefaction/sdk`

Shared TypeScript library: Ergo token registry helpers, wallet connectors (Nautilus, Ergo Wallet), validation, transaction queue utilities.

```bash
npm install
npm run build
```

Consumed by the frontend via SvelteKit path alias (`svelte.config.js`).
